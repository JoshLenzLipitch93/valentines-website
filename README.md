# Valentine’s Website

Built from the Figma design (`02`) with a sliding-tile puzzle on the left.

## Run locally

```bash
npm install
npm run dev
```

Then open the local URL shown in the terminal.

## Build for deployment

```bash
npm run build
```

Output goes to `dist/`.

